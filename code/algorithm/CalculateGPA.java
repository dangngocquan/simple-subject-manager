package code.algorithm;

// import java.util.LinkedList;
// import java.util.List;

// import code.objects.Plan;
// import code.objects.Subject;

public class CalculateGPA {
    // public static List<Plan> getPredictivePlans(Plan rootPlan) {
    // int totalSubjectNumber = rootPlan.getSubjects().size();
    // List<Plan> plans = new LinkedList<Plan>(); // Predictive plans - result
    // String[] characterScores = new
    // String[rootPlan.getConversionTable().getCharacterScore().size() - 1];
    // double[] score4s = new double[characterScores.length]; // score in sys 4
    // for (int i = 0; i < characterScores.length; i++) {
    // characterScores[i] = rootPlan.getConversionTable().getCharacterScore().get(i
    // + 1);
    // score4s[i] = rootPlan.getConversionTable().getScore4().get(i + 1);
    // }
    // findScoreOfSubject(1, totalSubjectNumber, rootPlan, plans, characterScores,
    // score4s);
    // return plans;
    // }

    // public static void findScoreOfSubject(int positionSubject, int
    // totalSubjectNumber, Plan rootPlan, List<Plan> plans,
    // String[] characterScores, double[] score4s) {
    // if (positionSubject > totalSubjectNumber) {
    // List<Subject> tempSubjects = new LinkedList<>();
    // for (Subject:)
    // }

    // }
}
